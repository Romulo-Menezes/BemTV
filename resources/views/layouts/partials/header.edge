<header class="flex justify-between bg-lime-300">
  <div class="flex pl-10 pt-1">
    <a href="/" title="Home">
      <img src="BemTV.svg" width="50">
    </a>
  </div>
    <ul class="flex m-5 ">
      @if(auth.isLoggedIn)
        <a href="{{route('editProfile/create')}}">
          <li class="pr-10">Editar Perfil</li>
        </a>
        <a href="{{ route('sessions/destroy') }}">
          <li class="pr-10">Logout</li>
        </a>

      @else
        <a href="{{ route('sessions/create') }}">
          <li class="pr-10">Login</li>
        </a>
      @endif
    </ul>
</header>
