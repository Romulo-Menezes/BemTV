@layout('layouts/simple')
@set('title', 'Editar Perfil')

@section('body')
<div class="flex justify-center pt-10 mb-10">
  <div id="cardBG" class="w-full max-w-sm p-4 bg-white rounded-lg shadow-md sm:p-6 md:p-8">
    @!alertError()
    @!alertSuccess()
    <form class="space-y-6" method="POST" action="{{ route('user/update')}}">
        <h5 class="text-xl font-medium text-white">Editar perfil</h5>

        @!input({
          type: 'text',
          name: 'firstName',
          text: 'Editar nome',
          placeholder: auth.user.first_name,
          value: false,
          required: false })

        @!input({
          type: 'text',
          name: 'lastName',
          text: 'Editar sobrenome',
          placeholder: auth.user.last_name,
          value: false,
          required: false })

        @!input({
          type: 'email',
          name: 'email',
          text: 'Editar e-mail',
          placeholder: auth.user.email,
          value: false,
          required: false })

        @!input({
          type: 'password',
          name: 'password',
          text: 'Editar senha',
          placeholder: '••••••••',
          value: false,
          required: false })

        @!input({
          type: 'password',
          name: 'passwordConfirmation',
          text: 'Confirme sua senha atual',
          placeholder: '••••••••',
          value: false,
          required: true })

        <button type="submit" class="w-full text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center :bg-blue-600">Editar</button>
    </form>
  </div>
</div>
@endsection
