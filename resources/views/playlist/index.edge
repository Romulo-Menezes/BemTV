@layout('layouts/main')
@set('title', title)

@section('body')
@!alertSuccess()
@!alertError()

<div class="flex flex-wrap items-center justify-center">
  @each((video, index) in videos)
  @!video.cardH({
    id: video.id,
    url_code: video.url_code,
    title: video.title,
    views: video.views,
    creator: `${video.author.first_name} ${video.author.last_name}`,
    time: times[index]
  })
  @endeach
</div>
<div class="mb-6">
  @!pageNav({ data, sideLimit: 3, limit: 7,path, search })
</div>
@endsection
