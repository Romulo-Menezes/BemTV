@layout('layouts/main')
@set('title', 'Home')

@section('body')
@!alertSuccess()
@!alertError()

<div class="flex flex-wrap items-center justify-center">
  @each(row in videos)
  @!video.card({
    id: row.id,
    url_code: row.url_code,
    title: row.title.length > 45 ? row.title.substring(0,42) + '...' : row.title,
  })
  @endeach
</div>
<div class="mb-6">
  @!pageNav({data: videos, sideLimit: 3, limit: 7})
</div>
@endsection
